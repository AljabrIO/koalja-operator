# Adds namespace to all resources.
namespace: koalja-system

# Value of this field is prepended to the
# names of all resources, e.g. a deployment named
# "wordpress" becomes "alices-wordpress".
# Note that it should also match with the prefix (text before '-') of the namespace
# field above.
namePrefix: koalja-

# Labels to add to all resources and selectors.
#commonLabels:
#  someName: someValue

# Each entry in this list must resolve to an existing
# resource definition in YAML.  These are the resource
# files that kustomize reads, modifies and emits as a
# YAML string, with resources separated by document
# markers ("---").
resources:
- ../manager/namespace.yaml
- ../rbac/rbac_role.yaml
- ../rbac/rbac_role_binding.yaml
- ../agents/pipeline_agent.yaml
- ../agents/link_agent.yaml
- ../agents/task_agent.yaml
- ../manager/manager.yaml
- ../services/annotatedvalue_registry.yaml
- ../services/local-fs.yaml
- ../services/flex-s3.yaml
- ../services/s3-fs.yaml
- ../tasks/dbquery_executor.yaml
- ../tasks/filedrop_executor.yaml
- ../tasks/filesplit_executor.yaml
- ../tasks/jsonquery_executor.yaml
